@AGENTS.md

# Commits
- **Never stage or commit anything inside `/docs/` or the folder itself.** It is a working scratchpad for plans and research; the user manages it manually. When running `git add`, list files explicitly — never `git add .` or `git add -A` near `/docs/`.
